box:
  id: 3.5-slim 
  ports:
    - "5000"

build:
  steps:
    # first we want to run pip-install to install all the dependencies
    - pip-install
    # then we want to run a wercker step that watches your files and reloads
    # when changes are detected.
    - script:
        name: echo python information
        code: |
          echo "python version $(python --version) running"
          echo "pip version $(pip --version) running"
    - internal/shell:
        cmd: /bin/sh
        code:
          - pip install -r requirements_dev.txt
          - python setupt.py install
          - cd pntl
          - git clone https://github.com/baojie/senna.git
          - pntl -SE ./senna

    - internal/watch:
        reload: true

